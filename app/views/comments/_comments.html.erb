<h3>Comments</h3>
  <% commentable.comments.each do |comment| %>
    <% if comment.campaign_id == @campaign.id %>
      <div class="well">
        <%= comment.body %>
      </div>
    <% end %>
  <% end %>
